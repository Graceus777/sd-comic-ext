# Custom lettering fonts

Drop `.ttf`, `.otf`, or `.ttc` font files in this directory, then restart or
reload the Comic Generator extension. Each file appears in the Font Editor tab as
`Custom: filename`.

Only use fonts whose licenses permit your intended comic distribution. Font
files are intentionally not bundled with the extension.
